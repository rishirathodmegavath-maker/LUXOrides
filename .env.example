# Copy to .env and fill in — the Fleetovo backend's LAN IP (not localhost,
# since Expo Go runs on a physical device, not this machine), and the org id.
EXPO_PUBLIC_API_BASE_URL=http://172.16.1.196:8443
EXPO_PUBLIC_ORG_ID=demo
